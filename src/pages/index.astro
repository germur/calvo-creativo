---
import { Image } from 'astro:assets'

import { Quote } from 'astro-pure/advanced'
import { PostPreview } from 'astro-pure/components/pages'
import { getBlogCollection, sortMDByDate } from 'astro-pure/server'
import { Button, Icon, Label } from 'astro-pure/user'
import PageLayout from '@/layouts/BaseLayout.astro'
import ProjectCard from '@/components/home/ProjectCard.astro'
import Section from '@/components/home/Section.astro'
import SkillLayout from '@/components/home/SkillLayout.astro'
import avatar from '@/assets/avatar.png'
import config from '@/site-config'

// Ajusta estas “skills” a tu stack real
const seoData = ['Python', 'Pandas', 'SQL', 'Screaming Frog']
const frontend = ['Astro', 'TypeScript', 'Vite']
const infra = ['Vercel', 'WSL/Ubuntu', 'GitHub']

const MAX_POSTS = 10
const allPosts = await getBlogCollection()
const allPostsByDate = sortMDByDate(allPosts).slice(0, MAX_POSTS)
---

<PageLayout meta={{ title: 'Home' }} highlightColor="#659EB9">
  <main class="flex w-full flex-col items-center">
    <section class="animate mb-10 flex flex-col items-center gap-y-7" id="content-header">
      <Image
        src={avatar}
        alt="profile"
        class="h-28 w-auto rounded-full border p-1"
        loading="eager"
      />

      <div class="flex flex-col items-center gap-y-4">
        <h1 class="text-3xl font-bold">{config.author}</h1>
        <div class="flex flex-wrap justify-center gap-x-2 gap-y-2">
          <Label title="Medellín, CO">Medellín, CO</Label>
          <Label
            title="GitHub"
            as="a"
            href="https://github.com/germur"
            target="_blank"
          >
            <Icon name="github" class="size-5" slot="icon" /> GitHub
          </Label>
        </div>
      </div>

      {/* CTA principal */}
      <a
        href="/contact"
        class="flex flex-row items-center gap-x-3 rounded-full border bg-background px-4 py-2 text-sm shadow-sm transition-shadow hover:shadow-md"
      >
        <span class="relative flex items-center justify-center">
          <span class="absolute size-2 animate-ping rounded-full border border-green-400 bg-green-400 opacity-75"></span>
          <span class="size-2 rounded-full bg-green-400"></span>
        </span>
        <p class="font-medium text-muted-foreground">Disponible para consultoría</p>
      </a>

      {/* CTA secundario: 🎲 Case Studies */}
      <Button href="/case-studies" class="mt-2">
        <Icon name="time" slot="icon" class="size-4 mr-2" /> 🎲 Roll the dice & discover a success story
      </Button>
    </section>

    <div id="content" class="animate flex flex-col gap-y-10 md:w-4/5 lg:w-5/6">
      <Section title="About">
        <p class="text-muted-foreground">SEO técnico · Data & Automatización</p>
        <p class="text-muted-foreground">
          Ayudo a equipos a escalar SEO con <strong>automatización en Python</strong> (crawling,
          clustering, auditorías y reporting), y contenido que impacta negocio —sin hype.
        </p>
        <div class="flex flex-wrap gap-3 self-end">
          <Button title="Sobre mí" class="w-fit" href="/about" variant="ahead" />
          <Button title="Ver artículos" class="w-fit" href="/blog" variant="ahead" />
        </div>
      </Section>

      {allPostsByDate.length > 0 && (
        <Section title="Posts recientes">
          <ul class="flex flex-col gap-y-1.5 sm:gap-y-2">
            {allPostsByDate.map((p) => (
              <li class="flex flex-col gap-x-2 sm:flex-row">
                <PostPreview post={p} />
              </li>
            ))}
          </ul>
          <Button title="Todos los posts" class="w-fit self-end" href="/blog" variant="ahead" />
        </Section>
      )}

      <Section title="Web & proyectos">
        <div class="grid grid-cols-1 gap-3 sm:grid-cols-2">
          <ProjectCard
            href="/blog"
            heading="Blog"
            subheading="SEO + Python, sin humo"
            imagePath="/src/assets/projects/alex-tyson-2BAXJ7ha74s-unsplash.jpg"
          />
          <ProjectCard
            href="/about"
            heading="Sobre mí"
            subheading="Perfil, stack y herramientas"
            imagePath="/src/assets/projects/angelica-teran-Bk9hpaXHK4o-unsplash.jpg"
          />
          <ProjectCard
            href="https://github.com/germur"
            heading="GitHub"
            subheading="Código y experimentos"
            imagePath="/src/assets/projects/kseniia-zapiatkina-yATU3rg8tNI-unsplash.jpg"
          />
          <ProjectCard
            href="/case-studies"
            heading="Case Studies"
            subheading="Resultados y aprendizajes"
            imagePath="/src/assets/projects/wen-qiao-g_w8I64FiO0-unsplash.jpg"
          />
        </div>
      </Section>

      <Section title="Skills">
        <SkillLayout title="SEO & Data" skills={seoData} />
        <SkillLayout title="Frontend" skills={frontend} />
        <SkillLayout title="Infra & Others" skills={infra} />
      </Section>
    </div>

    <Quote class="mt-12" />
  </main>
</PageLayout>
